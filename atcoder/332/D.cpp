#include<bits/stdc++.h> 

using namespace std; 

int main(){
	int H, W;
        cin >> H >> W; 
	int x; 
	vector<vector<int>> A (H, vector<int> (W, 0));
	vector<vector<int>> B (H, vector<int> (W, 0));
	for(auto &i : A)
		cin >> i; 
	for(auto &j: B)
		cin >> j;
	
	return 0;
}



