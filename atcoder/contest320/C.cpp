#include <bits/stdc++.h> 

using namespace std; 

int main(){ 
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int N, maior = -1; 
    cin >> N;
    vector<string> numbers; 
    string s;
    for(int i=0;i<3;i++){
        cin >> s;
        numbers.push_back(s);
    }
    for(int j=0;j<N;j++){
    }
    cout << maior << endl;
    return 0;
}